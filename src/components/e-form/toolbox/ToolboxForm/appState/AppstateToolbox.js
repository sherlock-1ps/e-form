import { Typography } from '@mui/material'
const AppstateToolbox = () => {
  return (
    <main>
      <Typography variant='h5'>จัดการตัวแปร</Typography>
    </main>
  )
}

export default AppstateToolbox
