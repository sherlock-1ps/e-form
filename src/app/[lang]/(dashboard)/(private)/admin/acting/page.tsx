'use client'
import colorSchemes from '@/@core/theme/colorSchemes'
import SettingActing from '@/views/admin/acting/SettingActing'

const Page = () => {
  // const primaryColor = colorSchemes('default')?.light?.palette?.background?.backgroundBoard || 'white'

  return (
    <div>
      <SettingActing />
    </div>
  )
}

export default Page
